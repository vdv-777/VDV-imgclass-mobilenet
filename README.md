# 🩺 Классификатор таблеток на основе MobileNetV3

Проект решает задачу **классификации таблеток по изображениям**. Поддерживает работу с датасетами в двух форматах, обучение с ранней остановкой и инференс. Оптимизирован под запуск на маломощных устройствах.



## 📁 Структура проекта

vdv-imgclass-mobilenet/

├── config.yaml # ← конфигурация (создаётся автоматически)

├── ogyeiv2/ # ← датасет (Папка датасета в данном репозитории - пустая, оставлена для примера)

│ ├── train/ # ← если dataset_structure = "split"

│ └── test/

├── vdv-imgclass/ # ← изображения для инференса (.jpg, .png)

├── notebook.ipynb # ← основной код

├── meds_classifier.pt # ← обученная модель (лучшая версия)

├── vdv-imgclass.csv # ← выгрузка результатов инференса

├── requirements.txt # ← зависимости для развертывания модели

├── classes.txt # ← перечень классов для автономного запуска инференса без необходимости скачивания датасета

└── Readme.md

## ⚙️ Конфигурация (`config.yaml`)

При первом запуске создаётся автоматически. Основные параметры:

| Параметр | Описание |
|--------|--------|
| `debug_mode` | `false` — обучение на всем загруженном объеме данных, либо дебаг-режим с обучением на небольшой выборке |
| `debug_train_size` / `debug_val_size` | Размеры поднаборов в дебаг-режиме |
| `dataset_structure` | `"split"` (разделён на train/test) или `"flat"` (все классы в одной папке) |
| `target_val_acc` | Целевая точность валидации (%). При достижении — обучение останавливается |
| `data_dir` | Путь для сохранения модели (перезаписывается только при улучшении) |
| `batch_size` | Размер батча при обучении модели |
| `learning_rate` | Learning rate для обучения |
| `img_size` | Целевой размер картинки при подготовке к подаче в модель |
| `inference_dir` | Путь для размещения файлов, подаваемых на распознавание при инференсе |
| `model_save_path` | Путь сохранения и загрузки весов модели |



Пример содержимого `config.yaml`:

```yaml
debug_mode: false
debug_train_size: 500
debug_val_size: 100
data_dir: ogyeiv2
batch_size: 32
epochs: 10
learning_rate: 0.001
img_size: 224
inference_dir: vdv-imgclass
model_save_path: meds_classifier.pt
inference_output_csv: vdv-imgclass.csv
target_val_acc: 80.0
dataset_structure: split
```


## ✅ Требования к Python

- **Минимальная версия**: Python 3.8
- **Рекомендуемая версия**: Python 3.9 – 3.11
- Убедитесь, что используется **64-битная версия Python**

## ☁️ Подготовка ВМ для развертывания

**1) Скачиваем датасет**
https://www.kaggle.com/datasets/richardradli/ogyeiv2/data

**2) Устанавливаем Python-3.10.12**
wget https://www.python.org/ftp/python/3.10.12/Python-3.10.12.tgz
tar -xf Python-3.10.12.tgz
cd Python-3.10.12

./configure --enable-optimizations
make -j$(nproc)
sudo make altinstall

python3.10 --version

python3.10 -m venv p310p2

source mp310p2/bin/activate

**3) Устанавливаем зависимости, включая ipykernel и jupyterlab**
pip3 install -r requirements.txt

При локальном развертывании без удаленных ВМ - пункты 4-7 можно пропустить.

**4) Запускаем на локалхосте сервер jupyterlab**

jupyter-lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root
[I 2025-11-17 14:57:40.023 ServerApp]     http://127.0.0.1:8888/lab?token=your_token_here

**5) Открываем порты для доступа jupyterlab через обратный портфорвардинг**
### На Windows (в PowerShell или CMD)
ssh -L 8888:localhost:8888 vdv777@158.160.154.189

**6) Открываем jupyterlab в браузере, авторизуемся по полученному токену**
http://localhost:8888
**7) Можно приступать к запуску ноутбуков на ВМ с jupyterlab.**


## ▶️ Как запустить

1. **Установите зависимости**:

   ```bash
   pip install -r requirements.txt

2. **Распакуйте датасет в папку (например, ogyeiv2/)**
3. **Поместите изображения для инференса в папку vdv-imgclass/**
4. **Запустите все ячейки в notebook.ipynb, если хотите переобучить модель или только блок 5 для распознавания, используя текущие веса**
5. **Результаты инференса появятся в консоли и в файле vdv-imgclass.csv**✅


## 📊 Особенности

- 📈 **Прогресс обучения** с отображением:
  - Текущего лосса и точности
  - Скорости обработки (batch/s)
  - Оставшегося времени (ETA)
- 💾 **Модель сохраняется только при улучшении** `Val Acc` — всегда актуальная лучшая версия.
- 🧠 Используется лёгкая модель **MobileNetV3 Small** — подходит для мобильных устройств.
- 🧪 Поддержка **GPU (CUDA)** — автоматическое определение.
- 🧩 Гибкость: работает с датасетами **OGYEI** и любыми другими классификационными наборами.

## 📊 Итоги обучения на полном датасете (10 эпох, CUDA)

- **Общая точность (accuracy): `73.81%`** — модель демонстрирует работоспособный, хотя и не максимальный результат.  
- **Сложные для классификации таблетки**:  
  Наибольшее число ошибок допущено для `atorvastatin_teva_20_mg` и `jutavit_cink` (по 6 ошибок), а также `concor_10_mg`, `quamatel_40_mg`, `theospirex_150_mg` (по 5 ошибок). Вероятно, эти препараты визуально схожи с другими классами или представлены в неоднозначных ракурсах/освещении.  
- **Надёжно распознаваемые таблетки**:  
  **25 классов** были классифицированы **без единой ошибки**, включая `strepsils`, `urzinol`, `vita_c`, `voltaren_dolo_rapid_25_mg`, `zadex_60_mg` и другие. Это свидетельствует о высокой устойчивости модели к большинству образцов.  
- **Рекомендации**:  
  Для повышения общей accuracy целесообразно провести анализ изображений «проблемных» классов — возможно, требуется аугментация, балансировка датасета или дообучение с фокусом на наиболее сложных примерах.